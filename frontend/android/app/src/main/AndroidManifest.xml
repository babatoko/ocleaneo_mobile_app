<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- allowBackup="false" : Android sauvegarde et restaure automatiquement
         les préférences de l'app (SharedPreferences, donc @capacitor/preferences)
         sur le compte Google du salarié, y compris à travers une désinstallation.
         Repéré sur un « premier lancement après installation » qui ne l'était pas
         vraiment : le provider actif (ocleaneo_data_provider) et l'URL du serveur
         REST (ocleaneo_api_base_url — un reliquat d'avant l'existence du
         sélecteur odoo/rest/démo) avaient été restaurés depuis une ancienne
         installation, avec REST pointé sur l'instance Odoo réelle — un mélange
         de contrats (REST/JSON générique vs JSON-RPC Odoo) qui fait échouer
         toute connexion avec un message "Pas de connexion" trompeur. Pour un
         outil interne lié à un seul backend, restaurer d'anciens réglages
         locaux après une réinstallation n'est jamais souhaitable. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Lance l'app quand un badge NFC est approché, même si elle est fermée
                 (pointage sans avoir à ouvrir l'app manuellement au préalable). -->
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <!-- Sans ces deux permissions, le pont Capacitor demande bien
         navigator.geolocation.getCurrentPosition() via
         ActivityCompat.requestPermissions (BridgeWebChromeClient), mais
         Android refuse silencieusement toute permission absente du
         manifeste : la boîte de dialogue système ne s'affiche même pas, et
         l'appel échoue à chaque fois. Deux dégradations muettes en
         résultaient : services/geofence.ts ne pouvait jamais vérifier la
         présence sur site (checkGeofence() reçoit null, ce qui ne bloque
         jamais le pointage — mais ne le vérifie plus non plus), et
         services/osrm.ts ne pouvait jamais faire partir la Tournée de la
         position réelle du salarié. COARSE seule suffirait pour ces deux
         usages (aucun n'exige une précision métrique) ; FINE est ajoutée
         car l'API de géolocalisation du WebView ne distingue pas les deux
         niveaux à la demande — c'est Android qui arbitre selon ce qui est
         accordé. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
</manifest>
