services:
  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: ${VITE_API_URL:?set to the Odoo instance API base URL}
    ports:
      - "8080:80"
    restart: unless-stopped
